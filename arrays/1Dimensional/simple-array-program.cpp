#include <iostreasm>
#include <iomanip>
#include <string>
#include <thread>
#include <cmath>

using namespace std;
int main(){
    cout << "Welcome to the student average calculator"  << endl;
    cout << "please enter your name to proceed." << endl;
    string name;
    getline(cin, name);
    cout << "Welcome " << name << "Let's begin this" << endl;

    cout << "\n\n" << endl;
    cout << "enter the number of students in your class" << endl;
    int numberofstuds;
    getline(cin, stoi(numberofstuds));
    cout << "Enter the number of subjects you're been working with" << endl;
    int numberofsubs;
    getline(cin, stoi(numberofsubs));
    for (int i=0, )
}
//we have to create a 2d array map, not a linear one. I'll be back.
//I will do this.