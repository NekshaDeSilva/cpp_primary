void rect(int* ptr, int length, /* New pointer parameter here */) {
    int w, h; 
    w = ptr[0];
    h = ptr[1];
    
    // Use dereferencing to set the value at the address
    _________________________ = w * h;
    
    // The function no longer needs to return the area
}           